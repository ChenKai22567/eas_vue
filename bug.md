1、var定义规则需要放在data开头，原因不明【解决】
2、padding更改右侧左侧反而变化
3、switch开关下方显示不全——删除组件自带属性style="display: block" 【解决】
4、三个图标原本间距不一致，过几天变好了 【怀疑是分配大小不足】
5、_ob_错误：data中return需要有数据，否则删掉。【解决】
6、$confirm取消时报错，使用catch捕获error信息